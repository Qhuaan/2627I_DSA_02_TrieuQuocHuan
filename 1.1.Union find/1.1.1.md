- Lỗi sai: Đoạn côde so sánh leader[i] == leader[p] trực tiếp bên trông vòng lặp. Giá trị của leader[i] được cập nhật thành giá trị của leader[q] ngay khi i=p, các phần tử i > p đáng nhẽ có cùng nhóm với p ban đầu sẽ không được cập nhật nữa vì leader[p] đã mang giá trị mới.
-Testcase:
Chọn n=3. Ban đầu leader=[0,1,2]
union(0,1). leader=[1,1,2]
union(0,2):
Khi i=0, leader[0] == leader[0] ( vì 1==1) dẫn đến leader[0] chuyển thành leader[2] là 2
Khi i=1, leader[1] đang là 1 nhưng lúc này leader[0] ( tức là leader[p]) đã thành 2. So sánh 1==2 dẫn đến leader[1] giữ nguyên là 1
Sau lệnh leader=[2,1,2]